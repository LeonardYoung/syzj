import { PasswordValidtorDirective } from './password-validtor.directive';

describe('PasswordValidtorDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordValidtorDirective();
    expect(directive).toBeTruthy();
  });
});
